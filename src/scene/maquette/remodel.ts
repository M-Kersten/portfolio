// TEMPORARY — the remodel proposal. Which objects render their new model;
// empty in production, filled by the model lab from its URL so the old and new
// versions can be rendered side by side. Goes away once the picks are in.
export const REMODEL = new Set<string>();
